#include <stdio.h>
/**
 * main - Entry point
 *
 */

int main(void)
{
	char c;

	int d;

	c = 'a';
	while
		(d < 10) {
			putchar(d + '0');
		}
	while
		(c <= 'f') {
			putchard(c);
		}	c++
	putchar('\n');	
	return(0);
}

